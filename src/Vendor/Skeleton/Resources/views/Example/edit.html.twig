{% extends "@VendorSkeleton/layout.html.twig" %}

{% block body %}
    <form action="{{ url('example_edit', {'id': example.id}) }}" method="post" {{ form_enctype(exampleForm) }}>
        {{ form_widget(exampleForm) }}
    </form>
    <a href="{{ url('example_list') }}">{{ "example.back"|trans }}</a>
{% endblock body %}
